{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "clima": {
      "type": "local",
      "command": ["docker", "run", "--rm", "-i", "mcp-project:latest"],
      "enabled": true
    }
  }
}
